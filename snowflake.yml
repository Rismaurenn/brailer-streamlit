definition_version: 1

entities:
  braille_to_latin:
    name: braille_to_latin
    description: "Sistem bantu baca Braille ke Latin"
    artifacts:
      - path: ./
    configuration:
      streamlit:
        main_file: app.py

package:
  name: braille_to_latin_package
  distribution: internal
